-- Резерв у добовому знімку.
--
-- Та сама діра, що двічі була раніше — з фондами (0012) і вкладами
-- (0016), — і з тієї ж причини: знімок завели, коли четвертої сутності ще
-- не існувало. Без цієї колонки крива «Капітал» показувала б портфель без
-- резерву, тобто менший за фактичний.
--
-- Старі рядки лишаються з нулем. Це не спотворення, а факт: у ті дні
-- резерв у знімку ніхто не рахував. UI зобовʼязаний казати «тоді не
-- рахували», а не «тоді не було».
ALTER TABLE snapshots ADD COLUMN reserve_uah INTEGER NOT NULL DEFAULT 0;
