-- Комісія брокера за лот (мінорні одиниці валюти лота). За замовчуванням 0
-- (без комісії). Існуючі лоти лишаються з fee=0 — поведінка не змінюється.
ALTER TABLE lots ADD COLUMN fee INTEGER NOT NULL DEFAULT 0;
