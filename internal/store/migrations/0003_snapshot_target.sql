-- Діюча місячна ціль на день знімка (мінорні одиниці, UAH). Дає змогу
-- будувати лінію «план» історично: минуле не переписується, коли ціль
-- змінюють. Старі знімки лишаються з 0.
ALTER TABLE snapshots ADD COLUMN month_target_uah INTEGER NOT NULL DEFAULT 0;
