// Довідники значень, спільні для розділів.

/** Коди типів виплат із довідника НБУ. */
export const PAY_TYPES = { 1: "купон", 2: "погашення", 3: "дострокове" };
export const PAY_CLASS = { 1: "coupon", 2: "redemption", 3: "early" };

/** Види операцій із сертифікатами фондів. */
export const FUND_KIND = { buy: "купівля", sell: "продаж", dividend: "дивіденд" };

/** Розділи застосунку. Порядок — порядок вкладок.
 *
 * Кожен розділ відповідає рівно на одне питання: що робити зараз /
 * що в мене є / чим я ризикую / де мої гроші / куди це йде / як воно
 * налаштоване. Розділ, який не відповідає на своє питання одним
 * поглядом, — привід рознести його.
 *
 * «Ризик» — шоста вкладка, і заборони на неї більше немає: доти
 * «Портфель» ніс вісімнадцять карток під чотирма секціями, і половина з
 * них відповідала не на «що я маю», а на «чим я ризикую» — розклад по
 * брокерах і валютах, ребалансування, концентрація, драбина, ліквідність,
 * ставки, бенчмарк. Розріз іде по лінії, яку код провів сам:
 * info.js#kindmix прямо каже, що валютна ціль — це «в чому тримати», а
 * структура за видом — «чим ризикувати». Тобто структура тут уже мова
 * ризику, а не складу, і клала її в «Портфель» лише відсутність місця. */
export const TABS = [
  ["overview", "Огляд"],       // що робити зараз
  ["portfolio", "Портфель"],   // що в мене є
  ["risk", "Ризик"],           // чим я ризикую
  ["money", "Гроші"],          // де мої гроші
  // «План» і «Майбутнє» були двома вкладками, доки не з'ясувалось, що вони
  // малюють ті самі числа: month_target_uah тут і contrib_plan там — це
  // одне out.ContribM, а крива капіталу малювалась двічі. Тепер один
  // розділ: що заходить і куди це веде.
  ["plan", "План"],            // скільки заходить і куди це веде
  ["settings", "Налаштування"], // як воно налаштоване
];

/** Як банк виплачує відсотки. Живе тут, а не в розділі вкладів: та сама
    підпис потрібен і єдиній таблиці позицій, а розділ не має бути
    довідником для сусіднього розділу. */
export const PAYOUT_LABEL = { end: "у кінці строку", monthly: "щомісяця", quarterly: "щокварталу" };

/** Вид інструмента одним словом — для пігулки. Спільний, бо та сама
    пігулка стоїть і в таблиці позицій, і в порадах «Що купити». */
export const KIND_LABEL = {
  bond: "ОВДП", fund: "Фонд", deposit: "Вклад", reserve: "Резерв", npf: "НПФ",
};
