// Довідники значень, спільні для розділів.

/** Коди типів виплат із довідника НБУ. */
export const PAY_TYPES = { 1: "купон", 2: "погашення", 3: "дострокове" };
export const PAY_CLASS = { 1: "coupon", 2: "redemption", 3: "early" };

/** Види операцій із сертифікатами фондів. */
export const FUND_KIND = { buy: "купівля", sell: "продаж", dividend: "дивіденд" };

// Розділів тут більше немає. Дерево навігації переїхало в js/nav.js — і не
// заради розміру: цей файл є довідником ЗНАЧЕНЬ домену, а список розділів
// був тут єдиним записом іншої природи. Разом із ним поїхав і весь
// аргумент про те, чому розділи саме такі; шукати його треба там.

/** Як банк виплачує відсотки. Живе тут, а не в розділі вкладів: та сама
    підпис потрібен і єдиній таблиці позицій, а розділ не має бути
    довідником для сусіднього розділу. */
export const PAYOUT_LABEL = { end: "у кінці строку", monthly: "щомісяця", quarterly: "щокварталу" };

/** Вид інструмента одним словом — для пігулки. Спільний, бо та сама
    пігулка стоїть і в таблиці позицій, і в порадах «Що купити». */
export const KIND_LABEL = {
  bond: "ОВДП", fund: "Фонд", deposit: "Вклад", reserve: "Резерв", npf: "НПФ",
  goal: "Ціль",
};

/** Кошики, куди може піти надходження, — ДОЗВІЛ потоку (plan_flows.uses).
 *
 *  Порядок той самий, що на бекенді (domain.planUseOrder), і той самий, у
 *  якому розкладка їх наповнює: подушка → цілі → папери → пенсійний. Дві
 *  копії порядку розійшлися б тихо — форма показувала б галочки в одному
 *  порядку, а пігулки під рядком у іншому.
 *
 *  pill — наявний клас кольору; в «інвестицій» власного немає навмисно:
 *  це не один вид інструмента, а троє разом (папери, фонди, вклади), і
 *  позичити колір в одного з них означало б збрехати про решту двох.
 *
 *  Живе тут, а не в plan-flows.js, бо читачів двоє: форма потоку й форма
 *  позапланового надходження. */
export const PLAN_USES = [
  ["reserve", "резерв", "pill-reserve"],
  ["goals", "цілі", "pill-goal"],
  ["invest", "інвестиції", ""],
  ["npf", "пенсійний", "pill-npf"],
];

/** Вид інструмента як ГРУПА капіталу. Ключі ті самі, що в рядках ребалансу
    за видом ("bonds"/"funds"/…), тобто множина, а не одна позиція.

    Окремо від KIND_LABEL навмисно, попри те, що чотири значення з п'яти
    збігаються слово в слово. Там ключ — вид ОДНІЄЇ позиції ("bond"), і
    зводити дві мапи в одну довелось би перекладачем ключів, який мовчки
    ламався б на кожному новому виді. Споживачів двоє — «Структура за видом»
    і «Скільки чого за стратегією», — і два власні списки тих самих п'яти
    назв розійшлись би тихо. */
export const KIND_GROUP = {
  bonds: "ОВДП", funds: "Фонди", deposits: "Вклади", reserve: "Резерв", npf: "НПФ",
};

/** Валюти, з якими працює застосунок, — ЄДИНИЙ список.
 *
 *  Доти ця трійка була вписана руками в семи місцях чотирма різними
 *  способами: `["UAH","USD","EUR"].map(...)` у трьох формах, локальний
 *  `curOpts()` у «Грошах», голі `<option>UAH` у вкладах, і ще раз як
 *  перевірка `.includes()` при автозаповненні з довідника ОВДП. Сім копій
 *  однієї трійки не помиляються, поки їх не треба міняти.
 *
 *  Це КОНСТАНТА ДОМЕНУ, а не запис у базі, і ендпойнта для неї немає
 *  навмисно: бекенд теж знає рівно ці три — курси тягне лише для USD і EUR
 *  (httputil.go), а реєстр налаштувань має deposit_min_{uah,usd,eur}
 *  окремими ключами. `GET /api/currencies` був би ендпойнтом із трьох
 *  літералів, який доводиться чекати, щоб намалювати форму.
 *
 *  Гривня перша: у ній ведеться облік, і саме вона типове значення там, де
 *  явного вибору немає. */
export const CURRENCIES = ["UAH", "USD", "EUR"];

/** Символ валюти. Живе поруч зі списком, а не в format.js, щоб нова валюта
 *  додавалась одним рядком в одному файлі. Читає його format.js:curSym —
 *  єдиний споживач, і саме через нього символ потрапляє в усі суми. */
export const CURRENCY_SYM = { UAH: "₴", USD: "$", EUR: "€" };
